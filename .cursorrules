# YOLO Mode Configuration

## Allowed Commands
The following commands are allowed to run automatically without user approval:

### Development Commands
- `npm test` - Run test suites
- `npm run dev` - Start development servers
- `npm run start` - Start production servers

### File System Commands
- `mkdir` - Create directories
- `mkdir -p` - Create directories with parent directories

### Package Management
- `npm install` - Install dependencies
- `npm install --save-dev` - Install dev dependencies
- `npm install --save` - Install production dependencies

### Build Commands
- `npm run build` - Build the application
- `npm run lint` - Run linting

## Safety Notes
- These commands are safe for development environments
- Directory creation is essential for project structure
- Test and dev commands are core development workflows
- Package installation is necessary for dependency management 